# Silicon Crafters — AMSR-Net Inference Requirements
# KLA Problem Statement: AI-Based Restoration of Degraded Images
#
# Install with:  pip install -r requirements.txt
#
# Notes:
# - torch/torchvision must be installed with CUDA support for GPU inference.
#   If using pip, install via:
#     pip install torch==2.1.2 torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu118
# - All other packages are CPU-side utilities and install from PyPI normally.

torch==2.1.2
torchvision==0.16.2
numpy==1.26.4
Pillow==10.3.0
scipy==1.13.0
tqdm==4.66.4
